# AI赋能职教视频个性化教学项目需求规格说明书（第六稿-完整版）

## 文档版本信息
- **版本号**: v6.0
- **编写日期**: 2026年1月
- **编写团队**: 周东吴（组长）、成怡、杨雅娟、张亚鹏、雷宇
- **技术顾问**: 蒋国伟
- **指导老师**: 王海霞
- **文档状态**: 待评审
- **版本说明**: 基于v4.0进行逻辑漏洞修复、功能完善与职教特色增强

---

## 版本演进说明

### v6.0 主要更新内容

| 更新类别 | 更新内容 | 影响模块 |
|---------|---------|---------|
| 逻辑重构 | 从"数据驱动"转向"认知+情境驱动" | 模块B |
| 逻辑修复 | 多模态交叉验证、负反馈闭环、时间戳动态修正 | 模块A/B/C |
| 职教增强 | 安全熔断机制、动作偏差分析、虚实结合 | 模块A/D |
| 干预优化 | 分层化干预策略（L1/L2/L3） | 模块C |
| 新增功能 | 认知负荷预警、社会学习模块、教练模式 | 模块C/D |
| 技术升级 | BERT语义引擎、Wav2Vec语气分析、GNN知识图谱 | AI引擎 |

---

## 目录
1. [项目概述](#1-项目概述)
2. [目标用户与使用场景](#2-目标用户与使用场景)
3. [核心功能模块详细设计](#3-核心功能模块详细设计)
4. [逻辑漏洞修复方案](#4-逻辑漏洞修复方案)
5. [职教特色增强设计](#5-职教特色增强设计)
6. [干预机制分层化设计](#6-干预机制分层化设计)
7. [系统架构与技术方案](#7-系统架构与技术方案)
8. [数据模型设计](#8-数据模型设计)
9. [接口设计](#9-接口设计)
10. [用户界面设计](#10-用户界面设计)
11. [非功能性需求](#11-非功能性需求)
12. [测试与验收标准](#12-测试与验收标准)
13. [项目里程碑与任务规划](#13-项目里程碑与任务规划)
14. [风险与对策](#14-风险与对策)
15. [附录](#15-附录)

---

# 1. 项目概述

## 1.1 项目一句话描述

构建一个**以主动智能补偿为核心、认知+情境驱动**的教学视频个性化学习支持系统，通过多模态分析学习者的观看行为与认知状态，**精准识别知识薄弱点并分层主动推送高质量补偿资源**，实现真正意义上的"一人一策"个性化教学。

## 1.2 项目背景

本项目是在蒋国伟团队前期"教学视频解析"成果基础上，进行深度的功能进阶与目标重构。项目聚焦职业教育领域，旨在解决以下核心痛点：

- **学生被动看视频**：学习者大量依赖教学视频自学，但普遍缺乏针对性反馈
- **知识吸收效率低**：学习过程中常见行为包括暂停、回放、拖拽返回、长时间停留等，这些行为往往对应"知识点未掌握/理解困难"，但传统教学难以实时捕捉
- **行为归因不精准**：传统系统无法区分"深度思考"与"走神/离开"，导致误判
- **学习路径一刀切**：传统平台"千人一面"，无法提供主动、精准、即时的补偿
- **教师负担重**：教师无法实时掌握每位学生的具体卡点，学情监测成本高
- **职教安全风险**：高危实操场景缺乏安全预警机制

## 1.3 核心愿景

- **懂学生的AI**：打造"教育版淘宝"的千人千面推荐能力，实现学习内容与路径的个性化匹配
- **认知驱动**：从单纯的行为数据分析升级为认知状态+情境感知的智能判定
- **安全优先**：职教场景下的安全熔断机制，保障实操学习安全
- **动态干预**：让AI从单纯的视频播放器，升级为能实时捕捉学习障碍、分层提供"补偿教育"的智能导师

## 1.4 项目目标（优先级排序）

### 1.4.1 核心目标

1. **精准认知状态识别**（最高优先级）
   - 引入**信号不协和（Signal Dissonance）**检测
   - 区分"深度思考"、"困难卡顿"与"走神/离开"
   - 认知状态识别准确率目标：≥ 85%（v1.0）

2. **主动智能补偿**（最高优先级）
   - 系统必须在学习者"卡住"时**主动干预**，而非等他求助
   - 响应时间：行为检测 → 补偿推送 ≤ 3-5分钟
   - 分层干预，避免干预疲劳

3. **溯源式根本原因诊断**
   - 若连续失败，回溯前置知识点而非重复当前知识点
   - 基于GNN知识依赖图计算最短回溯路径

4. **职教安全保障**
   - 高危场景安全熔断机制
   - 动作规范性实时评分

5. **自动生成高质量补偿资源**
   - 补偿资源时长控制在90秒以内
   - 必须"**短、狠、准**"，直击痛点
   - 负反馈闭环保证资源质量

## 1.5 项目边界（本期不做/暂缓）

- ❌ 不做泛娱乐平台视频理解与推荐
- ❌ 不做完整LMS/教务系统替代，仅做"视频学习辅助闭环"
- ❌ 不追求全学科通用，优先聚焦**汽修（Auto Repair）**课程跑通闭环
- ❌ 不以"最强模型"作为指标，优先可用、可验证、可迭代
- ❌ 不做完整的用户注册登录系统（初期可简化）
- ❌ 不做移动端APP（优先Web端，但保留WebSocket同步协议）

---

# 2. 目标用户与使用场景

## 2.1 用户角色

### 2.1.1 学生（主要用户）
- **角色描述**：职业教育、中职、高职、应用型本科学生（知识类+实操类课程）
- **核心需求**：
  - 观看教学视频
  - 接收智能补偿资源（分层推送）
  - 完成诊断练习
  - 获得个性化学习路径建议
  - 查看个人学习进度与薄弱点分析
  - 参与社会学习（知识锦囊分享）

### 2.1.2 教师/教研（次要用户）
- **角色描述**：任课教师、教研人员
- **核心需求**：
  - 上传教学视频
  - 查看班级共性难点、学情热力点
  - 查看"全班共同盲点图表"
  - 审核/编辑AI生成的知识点与资源
  - 响应L3级人类介入请求
  - 优化教学设计与策略

### 2.1.3 项目管理员（可选）
- **角色描述**：系统管理员、课程维护人员
- **核心需求**：
  - 课程/视频/资源维护
  - 策略阈值配置
  - 用户权限管理
  - 系统监控与日志查看
  - 安全规范库管理

## 2.2 核心使用场景（用户故事）

### 场景1：学生观看视频遇到难点（认知状态精准识别）

**用户故事**：学生观看《汽修-发动机拆装》视频，在"曲轴拆卸"知识点处暂停超过10秒。

**系统行为（v6.0升级）**：
1. 系统检测到 `Behavior_Pause = True`
2. 启动 **3秒窗口期扫描**，进行多模态交叉验证：
   - 摄像头检测：视线是否在屏幕知识点区域？
   - 鼠标行为：是否在图表/关键区域悬停？
   - 微表情分析：是否表现为"专注/轻微皱眉"？
3. **认知状态判定**：
   - ✅ 深度思考（Deep Thinking）：视线在区域 + 鼠标悬停 + 专注表情 → 不干预，记录正向行为
   - ⚠️ 困难卡顿（Difficulty）：视线在区域 + 回放行为 + 困惑表情 → 触发L1干预
   - ❌ 无效停留（Off-task）：视线偏移>5s + 无微操作 + 人体缺位 → 暂停打卡验证
4. 若判定为"困难卡顿"，在5分钟内主动推送L1级补偿资源

**学生反馈闭环**：
- 资源下方显示"是否有帮助？"按钮
- 若连续3名学生标记"无帮助"，自动下架该资源

### 场景2：溯源式根本原因诊断

**用户故事**：学生在"曲轴拆卸"知识点测验中连续失败2次。

**系统行为（v6.0升级）**：
1. 检测到连续失败2次
2. **逻辑切换**：不再重复推送"曲轴拆卸"资源
3. **回溯诊断**：
   - 查询GNN知识依赖图
   - 计算最短回溯路径：发现"曲轴拆卸"依赖"发动机结构认知"
   - 自动测试前置知识点"发动机结构认知"
4. 若前置知识点测试也失败 → 继续回溯
5. 找到根本薄弱点后，生成针对性补偿路径

### 场景3：高危场景安全熔断

**用户故事**：学生观看《焊接操作》视频。

**系统行为（v6.0新增）**：
1. 视频播放前，CV模块（YOLOv8）实时检测PPE（个人防护装备）
2. **检测内容**：
   - 护目镜佩戴状态
   - 防护手套佩戴状态
   - 工作服穿着规范
   - 操作姿势安全性
3. **安全熔断触发**：
   - 若识别到未戴护目镜或操作违规
   - 系统立即挂起当前视频
   - 强制弹出"安全规范确认"或"事故案例回顾"
   - 通过确认后方可继续播放
4. **动作评分**：
   - 使用SlowFast提取动作轨迹
   - 与标准库进行DTW（动态时间规整）比对
   - 输出《动作偏差分析报告》（如：焊接速度偏快15%，角度偏离5°）

### 场景4：认知负荷预警

**用户故事**：学生在短时间内触发3次以上补偿资源推送。

**系统行为（v6.0新增）**：
1. 检测到认知过载信号
2. 系统判定为"认知负荷过高"
3. **强制干预**：
   - 暂停新知识推送
   - 弹出"建议休息5分钟"提示
   - 或引导回顾基础前置知识点
4. 防止因过度学习导致的认知疲劳

### 场景5：社会学习（知识锦囊）

**用户故事**：学生A通过L2级干预掌握了"曲轴拆卸"知识点。

**系统行为（v6.0新增）**：
1. 检测到学生A从"困难"转为"掌握"
2. 系统邀请学生A录制30秒经验语音
3. 语音内容审核后，加入"知识锦囊"库
4. 当学生B卡在同一知识点时：
   - 优先推送同伴的经验分享
   - 增强社会学习效果

### 场景6：教师查看班级学情（教练模式）

**用户故事**：教师登录系统，查看《汽修-发动机拆装》课程的班级学情。

**系统展示（v6.0升级）**：
1. **全班共同盲点图表**：
   - 显示50%以上学生触发补偿的知识点
   - 自动生成"建议线下重点讲解"标签
2. **学情热力图**：
   - 每个知识点的认知状态分布
   - 深度思考/困难卡顿/无效停留比例
3. **补偿资源效果概览**：
   - 各级干预的成功率统计
   - 需要人工介入的资源列表

**教师行动**：
- 根据"全班共同盲点"优化后续面授课内容
- 响应L3级人类介入请求
- 审核下架的AI生成资源

---

# 3. 核心功能模块详细设计

## 3.1 模块A：视频多模态语义解析与知识建模（基石）

### 3.1.1 模块目标
把视频内容结构化到"知识点-时间段-关键词-资源-安全规范"层面，为后续的个性化推荐提供准确的内容语义基础。

### 3.1.2 输入
- **教学视频文件/链接**（优先：汽修类操作+讲解视频）
- **可选辅助材料**：
  - 原始字幕文件（SRT/VTT格式）
  - PPT文件（PPTX格式）
  - **实操手册PDF**（v6.0新增）
  - 讲义文本（TXT/DOCX格式）
  - 课程大纲/知识点列表
  - **标准动作库**（v6.0新增）

### 3.1.3 处理流程

#### A3.1 多源特征提取

**1. 音频处理（ASR + 语气分析）**
- **功能**：将教师讲解语音转为文本
- **输出**：带时间戳的转写文本 + **考核重点标记**（v6.0新增）
- **技术要求**：
  - 支持中文语音识别
  - 识别准确率 ≥ 90%
  - 支持方言与行业术语适配（可导入自定义词表）
  - 识别语音中的重音、停顿等强调性特征
  - **集成Wav2Vec 2.0**：通过识别教师语气起伏（Energy/Pitch）自动标记"考核重点"（v6.0新增）
  - 支持教师一键修正识别错误

**2. 视觉处理（CV/OCR + 安全检测）**
- **功能**：识别教学视频中的视觉信息
- **识别内容**：
  - PPT文字内容
  - 黑板板书
  - 实验演示关键动作
  - 图表、公式、代码片段
  - **PPE检测**（护目镜、手套、工作服）（v6.0新增）
  - **安全违规行为检测**（v6.0新增）
- **技术要求**：
  - OCR识别准确率 ≥ 85%
  - 支持中英文混合识别
  - 对模糊画面进行图像增强预处理
  - 自动检测视频清晰度、音量稳定性等质量问题
  - **集成YOLOv8进行PPE实时检测**（v6.0新增）

**3. 文本处理（语义引擎升级）**
- **功能**：解析视频字幕、标题、描述等元数据
- **技术升级**（v6.0）：
  - **使用BERT-base-Chinese**进行职业术语纠偏
  - 提取核心知识点
  - 识别关键术语
  - 分析文本语义结构

**4. 实操手册解析（v6.0新增）**
- **功能**：解析配套的PDF实操手册
- **输出**：
  - 安全规范条目（与视频时间戳关联）
  - 参数配置表（与操作步骤关联）
  - 工具清单（与动作片段关联）

#### A3.2 知识点切分与标注

**输出格式**：知识点列表 K = {K1, K2, ..., Kn}

**每个知识点Ki包含**：
- **基本信息**：
  - 知识点ID（唯一标识）
  - 知识点名称（可自动生成+人工可编辑）
  - 所属视频ID
  - 起止时间段（start_time, end_time，精确到秒）
  - **动态时间戳**（v6.0新增）：基于用户行为自动修正
- **语义信息**：
  - 关键词列表（top N，如10-20个）
  - 简要摘要（1-3句话，自动生成）
  - 知识点类型（概念/技能步骤/例题/实操等）
  - 难度等级（基础/进阶/拓展，可自动标注+人工调整）
  - **考核重点标记**（v6.0新增）：基于教师语气分析
- **关联信息**：
  - 前置知识点列表（依赖关系）
  - 后置知识点列表
  - 重要程度评分（1-5分）
  - **安全规范关联**（v6.0新增）：关联的安全条目
  - **手册章节关联**（v6.0新增）：对应的实操手册内容

**知识点粒度要求**：
- **细粒度知识点**（强烈建议）
  - 例如："曲轴拆卸步骤"、"千斤顶使用规范"、"机油更换时机判断"
  - 而非粗粒度章节（如"发动机维修"）
- **数量要求**：对选定课程样例视频，能生成不少于10-30个知识点切片

#### A3.3 知识图谱构建（GNN升级）

**基础要求**：
- 至少支持"顺序关系"（知识点在视频中的先后顺序）
- 支持知识点间的层级关系（父子节点）

**v6.0升级要求**：
- **使用GNN维护知识依赖图**（Knowledge Dependency Graph）
- 支持最短回溯路径计算
- 自动识别"前置知识缺失"场景
- 为"溯源式诊断"提供图结构支持

#### A3.4 时间戳动态修正（v6.0新增）

**问题背景**：职教视频中常存在"操作与讲解异步"问题，静态时间戳往往不准确。

**修正逻辑**：
1. 记录多数学生在点击某知识点后实际倒退/快进到的具体时间点
2. 统计分析行为数据，计算实际有效时间范围
3. 自动修正该知识点在图谱中的起始和结束坐标
4. 修正阈值：当超过30%的学生行为偏离原时间戳时触发修正

#### A3.5 职教专属功能

**1. 职教专属术语库构建**
- 针对汽修、数控、焊接、护理等细分领域，建立行业专属术语库
- 提升ASR/CV识别准确率
- 支持教师手动补充术语并迭代优化

**2. 实操动作规范性评分（v6.0升级）**
- 使用**SlowFast**提取动作轨迹
- 与标准动作库进行**DTW（动态时间规整）**比对
- 输出《动作偏差分析报告》：
  - 动作速度偏差（如：偏快15%）
  - 动作角度偏差（如：偏离5°）
  - 动作顺序偏差
  - 改进建议

**3. 安全规范实时关联（v6.0新增）**
- 当学生观看某操作动作时
- 侧边栏自动高亮显示手册中对应的"安全规范"和"参数配置"
- 实现"虚实结合"的学习体验

**4. 视频内容去娱乐化过滤**
- 自动识别并过滤与职教无关的娱乐化片段（闲聊、广告）
- 对非核心教学内容（点名、设备调试）智能分段
- 支持学生快速跳过

### 3.1.4 输出
- 结构化知识图谱JSON文件
- 知识点-时间戳索引表（动态更新）
- 知识点关系图（可视化）
- 安全规范关联表
- 动作偏差分析报告模板

### 3.1.5 验收标准
- ✅ 对选定课程样例视频，能生成不少于N个知识点切片（N由课程确定，如10-30）
- ✅ 每个知识点均具备可回放定位的时间戳（支持动态修正）
- ✅ 知识点识别准确率 ≥ 85%（v1.0）
- ✅ 支持人工在管理端调整知识点边界与名称（保证可控性）
- ✅ 知识点名称、摘要、关键词可人工编辑
- ✅ 安全规范正确关联率 ≥ 90%（v6.0新增）
- ✅ 动作偏差分析报告可生成（v6.0新增）

---

## 3.2 模块B：学习行为采集与认知状态识别（v6.0重构）

### 3.2.1 模块目标
从单纯的行为数据分析升级为**认知+情境驱动**的智能判定，精准区分"深度思考"、"困难卡顿"与"无效停留"。

### 3.2.2 行为数据采集（MVP必须）

#### B2.1 基础行为事件
系统实时记录以下行为事件（带时间戳）：

| 事件类型 | 描述 | 采集字段 |
|---------|------|---------|
| **play** | 开始播放 | video_id, timestamp, play_position |
| **pause** | 暂停播放 | video_id, timestamp, pause_position, pause_duration |
| **seek** | 拖拽进度条 | video_id, timestamp, from_position, to_position |
| **rate_change** | 改变播放速度 | video_id, timestamp, old_rate, new_rate |
| **ended** | 播放结束 | video_id, timestamp, total_duration |
| **leave** | 中途退出 | video_id, timestamp, leave_position, watch_duration |

#### B2.2 多模态认知状态信号（v6.0新增）

| 信号类型 | 数据来源 | 采集内容 |
|---------|---------|---------|
| **视线追踪** | 摄像头（需授权） | 视线位置（屏幕区域）、视线偏移时长 |
| **微表情** | 摄像头（需授权） | 表情分类（专注/困惑/走神/皱眉） |
| **鼠标行为** | 前端采集 | 鼠标位置、悬停区域、点击频率 |
| **键盘行为** | 前端采集 | 打字速度（笔记区）、快捷键使用 |
| **环境音** | 麦克风（需授权） | 环境杂音判定（教学相关/非教学相关） |
| **人体检测** | 摄像头（需授权） | 人体存在、大动作偏移 |

#### B2.3 高级行为指标
基于基础事件计算的高级指标：

- **回放次数**：某时间段重复进入的次数
- **停留时长**：在某知识点区间停留总时长
- **暂停频率**：单位时间内的暂停次数
- **拖拽回放频率**：向后拖拽的次数
- **快进/跳过行为**：向前跳过的片段（反向信号，可能表示已掌握）
- **认知状态分布**：深度思考/困难卡顿/无效停留的时间占比（v6.0新增）

### 3.2.3 认知状态判定模型（v6.0核心重构）

#### B3.1 信号不协和检测（Signal Dissonance）

**废除**：单一时间阈值判别

**新逻辑**：当 `Behavior_Pause = True` 时，启动 **3秒窗口期扫描**

```
认知状态判定流程：
┌─────────────────────────────────────────────────────────────┐
│  检测到 Pause 事件                                           │
│         ↓                                                    │
│  启动 3s 窗口期扫描                                          │
│         ↓                                                    │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 多模态信号采集：                                        ││
│  │ - 视线位置（屏幕区域/偏移）                             ││
│  │ - 微表情（专注/困惑/走神）                              ││
│  │ - 鼠标行为（悬停区域/无操作）                           ││
│  │ - 环境音（教学相关/杂音）                               ││
│  │ - 人体检测（存在/缺位/大动作）                          ││
│  └─────────────────────────────────────────────────────────┘│
│         ↓                                                    │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 加权概率分类器（BehaviorAnalysisService）               ││
│  │                                                         ││
│  │ P(DeepThinking) = f(视线在区域, 鼠标悬停, 专注表情)     ││
│  │ P(Difficulty)   = f(视线在区域, 回放行为, 困惑表情)     ││
│  │ P(OffTask)      = f(视线偏移>5s, 无操作, 人体缺位)      ││
│  └─────────────────────────────────────────────────────────┘│
│         ↓                                                    │
│  输出：认知状态分类 + 置信度                                 │
└─────────────────────────────────────────────────────────────┘
```

**判定结果分类**：

| 认知状态 | 判定条件 | 系统响应 |
|---------|---------|---------|
| **深度思考** | 视线在知识点区域 + 鼠标在图表悬停 + 专注/轻微皱眉 | 不干预，记录正向行为 |
| **困难卡顿** | 视线在区域 + 回放行为 + 困惑表情 | 触发分层干预（L1/L2/L3） |
| **无效停留** | 视线偏移>5s + 无微操作 + 环境杂音/人体缺位 | 暂停播放，打卡验证 |

#### B3.2 多模态交叉验证（v6.0新增）

**目的**：防止误判"走神"或临时离开为学习困难

**验证逻辑**：
1. 当检测到长时间停留时
2. 交叉验证以下条件：
   - 视线是否在屏幕内？
   - 设备是否有微操作（鼠标移动/点击）？
3. 只有两者均为"否"时，才判定为"非学习状态"
4. 触发"暂停/打卡验证"，避免影响学习画像

#### B3.3 困难度分数计算（升级版）

```python
# v6.0 困难度计算公式
Di = (
    w1 × cognitive_confusion_score +      # 认知困惑得分（微表情）
    w2 × behavior_difficulty_score +      # 行为困难得分（回放/暂停）
    w3 × knowledge_dependency_score +     # 知识依赖得分（前置缺失）
    w4 × time_anomaly_score              # 时间异常得分（停留比）
)

# 权重配置（可调）
w1 = 0.3  # 认知信号权重
w2 = 0.3  # 行为信号权重
w3 = 0.2  # 知识依赖权重
w4 = 0.2  # 时间异常权重
```

#### B3.4 公共难点识别
- 若某视频片段被多人（如≥5人）标记为"困难卡顿"，自动标记为"公共难点"
- 提升该知识点的优先级，供教师重点关注
- 自动加入"全班共同盲点图表"

#### B3.5 溯源式根本原因诊断（v6.0新增）

**触发条件**：学生在某知识点 $K_n$ 的测验中连续失败2次

**诊断逻辑**：
1. 系统逻辑由"重复学习 $K_n$"切换为"回溯测试前置知识点 $K_{n-1}$"
2. 使用GNN查询知识依赖图
3. 计算路径最短回溯节点
4. 对回溯节点进行快速诊断测试
5. 若回溯节点也失败，继续向前回溯
6. 直到找到真正的"根本薄弱点"

```
溯源诊断流程：
K5(失败) → 回溯 → K4(测试) → 通过 → 问题在K5本身
                           → 失败 → 继续回溯 → K3(测试) → ...
```

### 3.2.4 输出

**学生-知识点掌握状态**：
- **未学**：尚未观看该知识点
- **学习中**：正在观看，认知状态正常
- **深度思考**：检测到专注行为，正向信号（v6.0新增）
- **疑难**：检测到困难卡顿，需要干预
- **已掌握**：完成补偿后，练习正确率达标
- **根本薄弱**：溯源诊断发现的真正薄弱点（v6.0新增）

**疑难知识点列表（v6.0升级）**：
```json
{
  "user_id": "U001",
  "video_id": "V001",
  "difficult_points": [
    {
      "kp_id": "K3",
      "kp_name": "曲轴拆卸",
      "difficulty_score": 0.85,
      "cognitive_state": "difficulty",
      "cognitive_confidence": 0.92,
      "triggers": [
        {"type": "replay", "count": 3, "threshold": 2},
        {"type": "micro_expression", "value": "confused", "confidence": 0.88},
        {"type": "gaze_pattern", "value": "fixation_on_diagram", "duration": 8.5}
      ],
      "root_cause_analysis": {
        "is_root": false,
        "root_kp_id": "K1",
        "root_kp_name": "发动机结构认知",
        "dependency_path": ["K3", "K2", "K1"]
      },
      "detected_at": "2024-01-15 10:30:00"
    }
  ]
}
```

### 3.2.5 验收标准
- ✅ 能对单个学生一次观看记录生成"疑难知识点TopK"及触发原因
- ✅ 认知状态分类准确率 ≥ 85%（v6.0新增）
- ✅ 教师端可查看班级层面的疑难分布（按知识点聚合）
- ✅ 难点判定必须给出触发原因（可解释性）
- ✅ 支持阈值可配置
- ✅ 溯源诊断能正确识别根本薄弱点（v6.0新增）

---

## 3.3 模块C：个性化学习路径生成与智能补偿（核心竞争力）

### 3.3.1 模块目标
识别疑难点后，系统**分层主动推送**补偿资源，并根据反馈动态调整学习路径，实现"一人一策"，同时避免干预疲劳。

### 3.3.2 补偿资源类型

#### C2.1 知识卡片（必须）
- **内容**：概念/公式/关键步骤（1屏可读）
- **格式**：图文混排，支持Markdown
- **特点**：短小精悍，直击痛点
- **关联**：自动关联实操手册的安全规范（v6.0新增）

#### C2.2 诊断练习（必须）
- **题型**：选择/填空/判断/简答（至少3题）
- **生成方式**：可自动生成+人工编辑
- **要求**：
  - 每道题带详细解析
  - 难度与知识点匹配
  - 支持自动判分
  - **支持溯源诊断题目**（测试前置知识点）（v6.0新增）

#### C2.3 精讲微视频
- **时长限制**：**不超过90秒**（v6.0调整）
- **内容**：针对该知识点的精讲
- **生成方式**：
  - 优先：原视频智能剪辑（基于时间戳切片并重组）
  - 备选：AI生成（Sora-style）
  - 兜底：**关键帧拼接 + TTS语音旁白**（v6.0新增）

#### C2.4 知识锦囊（v6.0新增）
- **内容**：同伴的30秒经验语音分享
- **来源**：成功掌握该知识点的学生录制
- **审核**：需教师/系统审核后上架

#### C2.5 其他资源类型（可选扩展）
- 例题讲解
- 知识图谱节点跳转
- 3D拆解动画（针对实操类）
- 思维导图
- 安全规范确认题（针对高危场景）（v6.0新增）

### 3.3.3 认知负荷预警（v6.0新增）

**触发条件**：学生在短时间内触发3次以上补偿资源推送

**处理逻辑**：
1. 系统判定为"认知过载"
2. 暂停新知识推送
3. **强制建议**：
   - "休息5分钟"提示
   - 或"回顾基础前置知识点"引导
4. 避免因过度学习导致的认知疲劳
5. 记录认知负荷事件，用于学习画像分析

### 3.3.4 补偿资源质量保障（v6.0新增）

**负反馈闭环机制**：
1. 每个补偿资源下方显示"是否有帮助？"按钮
2. 学生可标记"有帮助"/"无帮助"
3. **自动下架规则**：若连续3名学生标记为"无帮助"
   - 系统自动下架该AI生成资源
   - 通知教师进行人工介入
   - 资源进入"待审核"队列
4. **质量评分**：基于反馈计算资源有效性评分

### 3.3.5 学习路径表示（v6.0升级）

```json
{
  "user_id": "U001",
  "current_progress": {
    "video_id": "V001",
    "current_kp": "K5",
    "completion_rate": 0.65,
    "cognitive_load_status": "normal"
  },
  "difficult_points": [
    {
      "kp_id": "K3",
      "status": "compensated",
      "compensation_level": "L2",
      "compensation_completed": true,
      "exercise_score": 0.85,
      "root_cause": {
        "is_root": false,
        "root_kp_id": "K1",
        "backtrack_completed": true
      }
    }
  ],
  "next_suggestion": {
    "action": "continue_watch",
    "target_kp": "K6",
    "reason": "前置知识点已掌握，根本薄弱点已修复"
  },
  "learning_path": [
    {"kp_id": "K1", "status": "mastered", "order": 1, "was_root_cause": true},
    {"kp_id": "K2", "status": "mastered", "order": 2},
    {"kp_id": "K3", "status": "mastered", "order": 3, "compensation_level": "L2"},
    {"kp_id": "K4", "status": "skipped", "order": 4, "reason": "已掌握前置知识"},
    {"kp_id": "K5", "status": "learning", "order": 5}
  ],
  "social_learning": {
    "available_tips": ["K3_tip_001", "K3_tip_002"],
    "contributed_tips": ["K2_tip_005"]
  }
}
```

### 3.3.6 动态路径调整

**调整策略**：
- 跳过已掌握的基础环节，直接进入实操或难点环节
- **溯源式调整**：若检测到根本薄弱点，优先回补前置知识（v6.0新增）
- 根据学习反馈实时调整后续推荐顺序
- **认知负荷调整**：过载时降低推送频率和难度（v6.0新增）

### 3.3.7 验收标准
- ✅ 对触发疑难的知识点，系统能分层展示补偿入口
- ✅ 记录学生完成补偿的情况
- ✅ 学生完成练习后，系统会更新该知识点状态
- ✅ 响应时间：行为检测 → 补偿推送 ≤ 3-5分钟
- ✅ 补偿资源时长不超过90秒（v6.0新增）
- ✅ 负反馈闭环机制正常工作（v6.0新增）
- ✅ 认知负荷预警正常触发（v6.0新增）
- ✅ 溯源诊断能生成正确的回补路径（v6.0新增）

---

## 3.4 模块D：教师赋能工具链（v6.0升级为"教练模式"）

### 3.4.1 教师标注工作台
- **功能**：提供可视化界面，支持快速修正AI识别的知识点标签
- **操作**：
  - 调整知识点边界（起止时间）
  - 修改知识点名称、摘要、关键词
  - 补充术语库
  - 审核补偿资源
  - 标注知识点难度等级
  - **审核知识锦囊内容**（v6.0新增）
  - **管理安全规范库**（v6.0新增）

### 3.4.2 教学效果仪表盘（v6.0升级）
- **展示内容**：
  - 班级整体薄弱点排行
  - **全班共同盲点图表**（v6.0新增）：50%以上学生触发补偿的知识点
  - 每个知识点的"认知状态分布"可视化（深度思考/困难卡顿/无效停留）
  - 补偿资源使用量与效果
  - 干预效果统计（各级干预成功率）
  - 学生学习进度分布
  - **溯源诊断分析**：最常见的"根本薄弱点"（v6.0新增）
  - **安全预警统计**（v6.0新增）
- **数据导出**：支持导出Excel/PDF报告

### 3.4.3 人类介入响应（v6.0新增）
- **功能**：响应L3级干预请求
- **流程**：
  1. 收到学生"求助"请求
  2. 查看学生学习轨迹和困难点分析
  3. 选择响应方式：
     - 1对1连线答疑
     - 推荐同伴互助
     - 安排线下辅导
  4. 记录响应结果

### 3.4.4 一键生成课件
- **功能**：基于多模态解析结果，自动生成教学课件
- **包含内容**：
  - 知识点列表（带时间戳）
  - 关键截图/视频片段
  - 补偿资源链接
  - 学生薄弱点统计
  - **全班共同盲点**（v6.0新增）
  - **建议线下重点讲解内容**（v6.0新增）
- **格式**：PPT/PDF，减轻备课负担

---

## 3.5 模块E：用户管理与权限系统

### 3.5.1 用户角色
- **学生**：观看视频、接收补偿、完成练习、贡献知识锦囊
- **教师**：上传视频、查看学情、审核资源、响应L3干预
- **管理员**：系统配置、用户管理、数据维护、安全规范库管理

### 3.5.2 权限控制
- 学生只能查看自己的学习数据
- 教师可查看所教班级的学情数据
- 管理员拥有全部权限

---

## 3.6 模块F：数据统计与分析（扩展）

### 3.6.1 学习数据分析
- 个人学习报告（周报/月报）
- 知识点掌握率趋势
- 学习时长统计
- 薄弱点分析
- **认知状态分布分析**（v6.0新增）
- **溯源诊断历史**（v6.0新增）

### 3.6.2 教学效果分析
- 班级整体学习效果
- 知识点难度评估
- 补偿资源有效性评估
- 教学策略优化建议
- **全班共同盲点趋势**（v6.0新增）
- **各级干预效果对比**（v6.0新增）

---

# 4. 逻辑漏洞修复方案

## 4.1 行为归因的"排他性"逻辑修复

### 4.1.1 问题描述
原逻辑：如果学生停留时长过长，统一判定为"疑难"。
漏洞：无法区分"走神"、"临时离开"与"真正困难"。

### 4.1.2 修复方案

**引入多模态交叉验证**：

```python
def classify_pause_behavior(pause_event, multimodal_signals):
    """
    多模态交叉验证暂停行为
    """
    # 信号采集
    gaze_on_screen = multimodal_signals.get("gaze_on_screen", None)
    has_micro_operation = multimodal_signals.get("has_micro_operation", False)
    body_present = multimodal_signals.get("body_present", None)
    facial_expression = multimodal_signals.get("facial_expression", "unknown")
    
    # 排他性判定
    if gaze_on_screen is False and has_micro_operation is False:
        # 两者均为否，判定为非学习状态
        return "off_task", "暂停打卡验证"
    
    if facial_expression in ["confused", "frustrated"]:
        # 困惑表情 + 视线在屏幕 = 真正困难
        return "difficulty", "触发补偿干预"
    
    if facial_expression in ["focused", "slight_frown"]:
        # 专注表情 = 深度思考
        return "deep_thinking", "不干预，记录正向行为"
    
    # 默认处理
    return "uncertain", "继续观察"
```

### 4.1.3 技术实现要点
- 视线追踪：使用WebGazer.js或摄像头API
- 微操作检测：监听鼠标移动、点击、滚动事件
- 表情识别：使用face-api.js或TensorFlow.js
- 人体检测：使用PoseNet或MediaPipe

## 4.2 补偿资源的"生成-反馈"负反馈闭环修复

### 4.2.1 问题描述
原逻辑：AI生成补偿资源 → 直接推送
漏洞：缺少对生成内容质量的校验

### 4.2.2 修复方案

**实时反馈机制**：

```python
class ResourceFeedbackService:
    """
    补偿资源反馈服务
    """
    
    def __init__(self):
        self.negative_threshold = 3  # 连续负反馈阈值
    
    def record_feedback(self, resource_id, user_id, is_helpful):
        """记录用户反馈"""
        feedback = ResourceFeedback(
            resource_id=resource_id,
            user_id=user_id,
            is_helpful=is_helpful,
            created_at=datetime.now()
        )
        db.save(feedback)
        
        # 检查是否需要自动下架
        self._check_auto_remove(resource_id)
    
    def _check_auto_remove(self, resource_id):
        """检查是否触发自动下架"""
        recent_feedbacks = db.query(
            ResourceFeedback
        ).filter(
            resource_id=resource_id
        ).order_by(
            desc(created_at)
        ).limit(self.negative_threshold).all()
        
        # 连续N个负反馈
        if len(recent_feedbacks) >= self.negative_threshold:
            if all(not f.is_helpful for f in recent_feedbacks):
                self._remove_resource(resource_id)
                self._notify_teacher(resource_id)
    
    def _remove_resource(self, resource_id):
        """下架资源"""
        resource = db.query(Resource).get(resource_id)
        resource.status = "removed_auto"
        resource.removed_at = datetime.now()
        resource.removed_reason = "连续负反馈自动下架"
        db.save(resource)
    
    def _notify_teacher(self, resource_id):
        """通知教师介入"""
        notification = TeacherNotification(
            type="resource_removed",
            resource_id=resource_id,
            message="AI生成资源因连续负反馈已自动下架，请审核"
        )
        notification_service.send(notification)
```

## 4.3 知识点时间戳的"动态修正"逻辑修复

### 4.3.1 问题描述
原逻辑：静态时间戳
漏洞：职教视频中"操作与讲解异步"，静态时间戳不准确

### 4.3.2 修复方案

**用户行为辅助修正**：

```python
class TimestampCorrectionService:
    """
    时间戳动态修正服务
    """
    
    def __init__(self):
        self.correction_threshold = 0.3  # 30%用户偏离触发修正
        self.min_samples = 10  # 最小样本数
    
    def record_actual_position(self, kp_id, user_id, clicked_position, actual_position):
        """
        记录用户点击知识点后实际定位到的位置
        """
        record = UserSeekBehavior(
            kp_id=kp_id,
            user_id=user_id,
            expected_position=clicked_position,
            actual_position=actual_position,
            deviation=actual_position - clicked_position,
            created_at=datetime.now()
        )
        db.save(record)
        
        # 检查是否需要修正
        self._check_correction(kp_id)
    
    def _check_correction(self, kp_id):
        """检查是否触发时间戳修正"""
        behaviors = db.query(UserSeekBehavior).filter(kp_id=kp_id).all()
        
        if len(behaviors) < self.min_samples:
            return  # 样本不足
        
        # 统计偏离比例
        deviation_users = [b for b in behaviors if abs(b.deviation) > 5]  # 偏离超过5秒
        deviation_ratio = len(deviation_users) / len(behaviors)
        
        if deviation_ratio >= self.correction_threshold:
            # 计算新的时间戳
            new_start = self._calculate_new_timestamp(behaviors, "start")
            new_end = self._calculate_new_timestamp(behaviors, "end")
            
            # 更新知识点时间戳
            self._update_timestamp(kp_id, new_start, new_end)
    
    def _calculate_new_timestamp(self, behaviors, position_type):
        """计算新的时间戳（中位数法）"""
        positions = [b.actual_position for b in behaviors]
        return statistics.median(positions)
    
    def _update_timestamp(self, kp_id, new_start, new_end):
        """更新知识点时间戳"""
        kp = db.query(KnowledgePoint).get(kp_id)
        kp.start_time = new_start
        kp.end_time = new_end
        kp.timestamp_corrected = True
        kp.correction_count += 1
        db.save(kp)
```

---

# 5. 职教特色增强设计

## 5.1 高危场景"安全熔断"机制（v6.0新增）

### 5.1.1 应用场景
- 焊接操作视频
- 电工操作视频
- 化工实验视频
- 汽修高危操作视频（如千斤顶使用）

### 5.1.2 技术实现

**CV模块（YOLOv8）PPE检测**：

```python
class SafetyMonitorService:
    """
    安全监控服务
    """
    
    def __init__(self):
        self.ppe_detector = YOLOv8PPEDetector()
        self.action_validator = ActionValidator()
    
    def check_safety(self, video_frame, operation_type):
        """
        安全检查
        """
        # PPE检测
        ppe_result = self.ppe_detector.detect(video_frame)
        
        # 根据操作类型确定必需的PPE
        required_ppe = self._get_required_ppe(operation_type)
        
        # 检查是否佩戴必需PPE
        missing_ppe = []
        for ppe in required_ppe:
            if not ppe_result.has(ppe):
                missing_ppe.append(ppe)
        
        if missing_ppe:
            return SafetyCheckResult(
                is_safe=False,
                violation_type="missing_ppe",
                missing_items=missing_ppe,
                action="suspend_video"
            )
        
        # 动作违规检测
        action_result = self.action_validator.validate(video_frame, operation_type)
        if not action_result.is_valid:
            return SafetyCheckResult(
                is_safe=False,
                violation_type="unsafe_action",
                violation_detail=action_result.detail,
                action="suspend_video"
            )
        
        return SafetyCheckResult(is_safe=True)
    
    def _get_required_ppe(self, operation_type):
        """获取操作类型必需的PPE"""
        ppe_requirements = {
            "welding": ["goggles", "gloves", "apron", "helmet"],
            "electrical": ["gloves", "safety_shoes", "helmet"],
            "chemical": ["goggles", "gloves", "lab_coat", "mask"],
            "auto_repair": ["gloves", "safety_shoes", "coverall"]
        }
        return ppe_requirements.get(operation_type, [])
```

### 5.1.3 安全熔断流程

```
┌─────────────────────────────────────────────────────────────┐
│  视频播放                                                    │
│      ↓                                                       │
│  实时PPE检测（YOLOv8）                                       │
│      ↓                                                       │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 检测到违规？                                            ││
│  │   ├── 未戴护目镜                                        ││
│  │   ├── 未戴防护手套                                      ││
│  │   ├── 操作姿势危险                                      ││
│  │   └── 其他安全隐患                                      ││
│  └─────────────────────────────────────────────────────────┘│
│      ↓                                                       │
│  【安全熔断】立即挂起视频                                    │
│      ↓                                                       │
│  强制弹出安全确认                                            │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ ○ 安全规范确认（必须通过）                              ││
│  │ ○ 事故案例回顾（可选）                                  ││
│  └─────────────────────────────────────────────────────────┘│
│      ↓                                                       │
│  通过确认后继续播放                                          │
└─────────────────────────────────────────────────────────────┘
```

## 5.2 基于标准差的动作评分（v6.0新增）

### 5.2.1 技术方案

**废除**：二元"对/错"判定
**新逻辑**：动态时间规整（DTW）量化偏差

```python
class ActionScoringService:
    """
    动作评分服务
    """
    
    def __init__(self):
        self.slowfast_extractor = SlowFastExtractor()
        self.standard_library = StandardActionLibrary()
    
    def score_action(self, video_segment, action_type):
        """
        动作评分
        """
        # 提取动作轨迹
        action_trajectory = self.slowfast_extractor.extract(video_segment)
        
        # 获取标准动作轨迹
        standard_trajectory = self.standard_library.get(action_type)
        
        # DTW比对
        dtw_result = self._dtw_compare(action_trajectory, standard_trajectory)
        
        # 生成偏差分析报告
        report = ActionDeviationReport(
            action_type=action_type,
            overall_score=dtw_result.similarity_score,
            speed_deviation=dtw_result.speed_deviation,
            angle_deviation=dtw_result.angle_deviation,
            sequence_deviation=dtw_result.sequence_deviation,
            recommendations=self._generate_recommendations(dtw_result)
        )
        
        return report
    
    def _dtw_compare(self, trajectory, standard):
        """动态时间规整比对"""
        # 使用fastdtw库进行DTW计算
        distance, path = fastdtw(trajectory, standard, dist=euclidean)
        
        # 分析各维度偏差
        speed_deviation = self._analyze_speed(trajectory, standard, path)
        angle_deviation = self._analyze_angle(trajectory, standard, path)
        sequence_deviation = self._analyze_sequence(trajectory, standard, path)
        
        return DTWResult(
            distance=distance,
            similarity_score=1 / (1 + distance),
            speed_deviation=speed_deviation,
            angle_deviation=angle_deviation,
            sequence_deviation=sequence_deviation
        )
    
    def _generate_recommendations(self, dtw_result):
        """生成改进建议"""
        recommendations = []
        
        if abs(dtw_result.speed_deviation) > 0.1:
            if dtw_result.speed_deviation > 0:
                recommendations.append(f"操作速度偏快{dtw_result.speed_deviation*100:.1f}%，建议放慢节奏")
            else:
                recommendations.append(f"操作速度偏慢{abs(dtw_result.speed_deviation)*100:.1f}%，可适当加快")
        
        if abs(dtw_result.angle_deviation) > 5:
            recommendations.append(f"动作角度偏离{dtw_result.angle_deviation:.1f}°，注意姿势调整")
        
        return recommendations
```

### 5.2.2 动作偏差分析报告示例

```json
{
  "action_type": "曲轴拆卸",
  "overall_score": 0.82,
  "deviations": {
    "speed": {
      "value": "+15%",
      "recommendation": "拆卸速度偏快15%，建议放慢节奏确保安全"
    },
    "angle": {
      "value": "-5°",
      "recommendation": "工具握持角度偏离5°，注意调整手腕姿势"
    },
    "sequence": {
      "value": "correct",
      "recommendation": "步骤顺序正确"
    }
  },
  "safety_notes": [
    "注意：高速拆卸可能导致零件损坏或飞溅",
    "建议：在正式操作前多练习3-5次"
  ]
}
```

## 5.3 虚实结合的动作对齐（v6.0新增）

### 5.3.1 功能描述
当学生观看某个操作动作时，侧边栏自动高亮显示手册中对应的"安全规范"和"参数配置"。

### 5.3.2 技术实现

```python
class VirtualRealAlignmentService:
    """
    虚实结合对齐服务
    """
    
    def __init__(self):
        self.manual_parser = ManualParser()
        self.alignment_engine = AlignmentEngine()
    
    def get_aligned_content(self, video_id, current_timestamp):
        """
        获取当前视频时间点对应的手册内容
        """
        # 获取当前知识点
        kp = self._get_current_knowledge_point(video_id, current_timestamp)
        
        if not kp:
            return None
        
        # 获取关联的手册内容
        aligned_content = self.alignment_engine.get_alignment(kp.id)
        
        return VirtualRealContent(
            knowledge_point=kp,
            safety_regulations=aligned_content.safety_regulations,
            parameter_configs=aligned_content.parameter_configs,
            tool_list=aligned_content.tool_list,
            warnings=aligned_content.warnings
        )
```

### 5.3.3 界面展示

```
┌────────────────────────────────────────────────────────────────────┐
│  ┌─────────────────────────────────┐  ┌──────────────────────────┐│
│  │                                 │  │ 📖 实操手册对照          ││
│  │                                 │  │ ─────────────────────────││
│  │        视频播放器               │  │ 🔴 安全规范：            ││
│  │                                 │  │ • 必须佩戴防护手套       ││
│  │     【曲轴拆卸操作演示】        │  │ • 确保千斤顶稳固         ││
│  │                                 │  │ • 禁止在发动机运转时操作 ││
│  │                                 │  │ ─────────────────────────││
│  │                                 │  │ ⚙️ 参数配置：            ││
│  └─────────────────────────────────┘  │ • 扭矩值：45-50 N·m      ││
│                                       │ • 间隙：0.15-0.25 mm     ││
│                                       │ ─────────────────────────││
│                                       │ 🔧 工具清单：            ││
│                                       │ • 曲轴拆卸专用扳手       ││
│                                       │ • 扭矩扳手               ││
│                                       │ • 塞尺                   ││
│                                       └──────────────────────────┘│
└────────────────────────────────────────────────────────────────────┘
```

## 5.4 标准动作库科目选择

### 5.4.1 选择建议
**推荐科目**：**汽修（Auto Repair）**

### 5.4.2 选择理由
1. **动作标准明确**：拆装步骤有明确的行业标准
2. **视觉特征明显**：工具识别容易（扳手、千斤顶等）
3. **安全逻辑丰富**：千斤顶使用、高压系统操作等高风险场景多
4. **DTW适配性好**：动作轨迹易于提取和比对
5. **市场需求大**：汽修人才缺口大，应用前景广

---

# 6. 干预机制分层化设计（Anti-Fatigue Strategy）

## 6.1 分层干预策略

| 等级 | 触发条件 | 干预形式 | 资源优先级 | 响应时间 |
|------|---------|---------|-----------|---------|
| **L1: 轻量提醒** | 首次T1异常停留 | 侧边栏浮窗（一句话总结/知识卡） | 原创视频摘要 | ≤1分钟 |
| **L2: 交互修复** | 二次停留或测验失败 | 90秒精准微视频 + 针对性单选题 | AI智能剪辑片段 | ≤3分钟 |
| **L3: 人类介入** | 修复后持续低增益 | 触发"求助"按钮，推送至班级群或导师端 | 1对1连线/同伴互助 | ≤5分钟 |

## 6.2 各级干预详细设计

### 6.2.1 L1级：轻量提醒

**触发条件**：
- 首次检测到困难卡顿（T1异常）
- 认知状态判定为"difficulty"
- 置信度 ≥ 0.7

**干预内容**：
- 侧边栏浮窗提示
- 一句话知识点总结
- 或小型知识卡片（1屏可读）

**干预形式**：
- 非强制弹出，不打断学习
- 用户可点击展开或忽略
- 3秒后自动最小化

**成功标准**：
- 用户点击查看后继续正常学习
- 后续无再次触发T1异常

### 6.2.2 L2级：交互修复

**触发条件**：
- L1干预后仍触发T1异常
- 或测验失败（正确率 < 80%）
- 或溯源诊断发现前置知识缺失

**干预内容**：
- 90秒精准微视频
- 3道针对性单选题
- 知识锦囊（同伴经验分享）

**干预形式**：
- 侧边栏展开，可选择查看
- 完成练习后更新掌握状态
- 支持"跳过"（记录跳过行为）

**成功标准**：
- 练习正确率 ≥ 80%
- 后续学习无再次触发异常

### 6.2.3 L3级：人类介入

**触发条件**：
- L2干预后仍持续低增益
- 或连续3次测验失败
- 或学生主动点击"求助"按钮

**干预内容**：
- 推送求助请求至教师端
- 推送至班级学习群
- 推荐可以帮助的同伴

**干预形式**：
- 1对1连线答疑
- 同伴互助
- 线下辅导安排

**成功标准**：
- 教师/同伴介入后问题解决
- 后续学习恢复正常

## 6.3 防疲劳策略

### 6.3.1 干预频率控制
- L1干预：同一知识点最多2次
- L2干预：同一知识点最多1次
- L3干预：同一学习周期最多2次

### 6.3.2 认知负荷监控
- 短时间内（30分钟）触发3次以上补偿 → 认知过载
- 强制休息建议或基础回顾引导

### 6.3.3 渐进式干预
- 从轻到重，避免一开始就强干预
- 给学生自主解决问题的机会

---

# 7. 系统架构与技术方案

## 7.1 总体架构（v6.0升级）

````
┌─────────────────────────────────────────────────────────────┐
│                      前端层 (Web)                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ 学生端   │  │ 教师端   │  │ 管理端   │                  │
│  │ +多模态  │  │ +教练模式│  │ +安全管理│                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
└─────────────────────────────────────────────────────────────┘
                          │
                          │ HTTP/WebSocket（毫秒级同步）
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                      后端服务层                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐ │
│  │业务逻辑  │  │行为分析  │  │路径生成  │  │安全监控    │ │
│  │Service   │  │Service   │  │Service   │  │Service     │ │
│  └──────────┘  └──────────┘  └──────────┘  └────────────┘ │
└─────────────────────────────────────────────────────────────┘
                          │
                          │ API调用
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                      AI引擎层                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐ │
│  │多模态解析│  │认知状态  │  │资源生成  │  │动作评分    │ │
│  │+语气分析 │  │识别服务  │  │服务      │  │服务        │ │
│  └──────────┘  └──────────┘  └──────────┘  └────────────┘ │
└─────────────────────────────────────────────────────────────┘
                          │
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据存储层                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐ │
│  │MySQL/    │  │Redis     │  │MinIO/OSS │  │Neo4j       │ │
│  │PostgreSQL│  │缓存      │  │文件存储  │  │知识图谱    │ │
│  └──────────┘  └──────────┘  └──────────┘  └────────────┘ │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                      外部服务层                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐ │
│  │ASR API   │  │OCR API   │  │大模型API │  │视频生成API │ │
│  │讯飞/百度 │  │PaddleOCR │  │GPT-4V    │  │Sora/剪辑   │ │
│  └──────────┘  └──────────┘  └──────────┘  └────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## 7.2 技术栈声明（v6.0升级）

### 7.2.1 前端技术栈

| 组件 | 技术选型 | v6.0新增 |
|------|---------|---------|
| 框架 | Vue.js 3 + TypeScript | - |
| UI组件库 | Element Plus | - |
| 状态管理 | Pinia | - |
| 视频播放器 | Video.js / DPlayer | - |
| 可视化 | ECharts | - |
| **视线追踪** | WebGazer.js | ✅ |
| **表情识别** | face-api.js | ✅ |
| **实时通信** | WebSocket | ✅ |

### 7.2.2 后端技术栈

| 组件 | 技术选型 | v6.0新增 |
|------|---------|---------|
| Web框架 | FastAPI | - |
| ORM | SQLAlchemy | - |
| 数据库 | PostgreSQL | - |
| 缓存 | Redis | - |
| 消息队列 | RabbitMQ | - |
| **知识图谱** | Neo4j + GNN | ✅ |
| **实时推送** | WebSocket | ✅ |

### 7.2.3 AI引擎技术栈

| 组件 | 技术选型 | v6.0新增 | 用途 |
|------|---------|---------|------|
| 语义引擎 | **BERT-base-Chinese** | ✅ | 职业术语纠偏 |
| 语气分析 | **Wav2Vec 2.0** | ✅ | 考核重点标记 |
| ASR | 讯飞/百度/Whisper | - | 语音转文本 |
| OCR | PaddleOCR | - | 文字识别 |
| PPE检测 | **YOLOv8** | ✅ | 安全装备检测 |
| 动作识别 | **SlowFast** | ✅ | 动作轨迹提取 |
| 动作比对 | **DTW (fastdtw)** | ✅ | 动作偏差分析 |
| 知识图谱 | **GNN (PyTorch Geometric)** | ✅ | 依赖关系计算 |
| 大模型 | GPT-4V/Claude | - | 资源生成 |

### 7.2.4 视频生成备选策略

```
视频生成优先级：
1. 原视频智能剪辑（基于时间戳切片重组）
2. AI生成（Sora-style，限时90秒）
3. 兜底方案：关键帧拼接 + TTS语音旁白
```

### 7.2.5 同步协议

- **WebSocket**：确保PC端与移动端学习进度、微表情特征数据的毫秒级同步
- **心跳机制**：每5秒发送心跳包，确保连接稳定
- **断线重连**：自动重连机制，最多重试3次

---

# 8. 数据模型设计

## 8.1 核心实体（v6.0升级）

### 8.1.1 用户表 (User)
```sql
CREATE TABLE user (
    id VARCHAR(50) PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    role ENUM('student', 'teacher', 'admin') NOT NULL,
    class_id VARCHAR(50),
    profile JSON,
    -- v6.0新增
    camera_permission BOOLEAN DEFAULT FALSE,  -- 摄像头权限
    mic_permission BOOLEAN DEFAULT FALSE,     -- 麦克风权限
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 8.1.2 知识点表 (KnowledgePoint) - v6.0升级
```sql
CREATE TABLE knowledge_point (
    id VARCHAR(50) PRIMARY KEY,
    video_id VARCHAR(50) NOT NULL,
    title VARCHAR(200) NOT NULL,
    start_time INT NOT NULL,
    end_time INT NOT NULL,
    -- 动态时间戳（v6.0新增）
    corrected_start_time INT,
    corrected_end_time INT,
    timestamp_corrected BOOLEAN DEFAULT FALSE,
    correction_count INT DEFAULT 0,
    -- 语义信息
    keywords TEXT,
    summary TEXT,
    kp_type ENUM('concept', 'skill', 'example', 'practice'),
    difficulty_level ENUM('basic', 'intermediate', 'advanced'),
    importance_score INT,
    -- v6.0新增
    is_exam_focus BOOLEAN DEFAULT FALSE,      -- 考核重点（语气分析）
    safety_regulation_ids TEXT,               -- 关联安全规范
    manual_section_id VARCHAR(50),            -- 关联手册章节
    -- 关系
    parent_kp_id VARCHAR(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (video_id) REFERENCES video(id)
);
```

### 8.1.3 认知状态事件表 (CognitiveStateEvent) - v6.0新增
```sql
CREATE TABLE cognitive_state_event (
    id VARCHAR(50) PRIMARY KEY,
    user_id VARCHAR(50) NOT NULL,
    video_id VARCHAR(50) NOT NULL,
    kp_id VARCHAR(50),
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    -- 认知状态
    cognitive_state ENUM('deep_thinking', 'difficulty', 'off_task', 'uncertain') NOT NULL,
    confidence FLOAT,
    -- 多模态信号
    gaze_on_screen BOOLEAN,
    gaze_position JSON,
    facial_expression VARCHAR(50),
    expression_confidence FLOAT,
    has_micro_operation BOOLEAN,
    body_present BOOLEAN,
    environment_noise_level FLOAT,
    -- 触发的干预
    intervention_level ENUM('none', 'L1', 'L2', 'L3'),
    intervention_triggered BOOLEAN DEFAULT FALSE,
    FOREIGN KEY (user_id) REFERENCES user(id),
    FOREIGN KEY (video_id) REFERENCES video(id),
    INDEX idx_user_video (user_id, video_id),
    INDEX idx_cognitive_state (cognitive_state)
);
```

### 8.1.4 溯源诊断表 (RootCauseDiagnosis) - v6.0新增
```sql
CREATE TABLE root_cause_diagnosis (
    id VARCHAR(50) PRIMARY KEY,
    user_id VARCHAR(50) NOT NULL,
    trigger_kp_id VARCHAR(50) NOT NULL,        -- 触发诊断的知识点
    root_kp_id VARCHAR(50),                    -- 根本薄弱知识点
    dependency_path JSON,                       -- 回溯路径
    diagnosis_status ENUM('in_progress', 'completed', 'failed'),
    backtrack_depth INT,                        -- 回溯深度
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES user(id),
    FOREIGN KEY (trigger_kp_id) REFERENCES knowledge_point(id),
    FOREIGN KEY (root_kp_id) REFERENCES knowledge_point(id)
);
```

### 8.1.5 安全规范表 (SafetyRegulation) - v6.0新增
```sql
CREATE TABLE safety_regulation (
    id VARCHAR(50) PRIMARY KEY,
    title VARCHAR(200) NOT NULL,
    content TEXT NOT NULL,
    operation_type VARCHAR(50),                 -- 操作类型（焊接/电工/汽修等）
    required_ppe JSON,                          -- 必需的PPE列表
    risk_level ENUM('low', 'medium', 'high', 'critical'),
    accident_case_id VARCHAR(50),               -- 关联事故案例
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 8.1.6 安全熔断事件表 (SafetyBreakEvent) - v6.0新增
```sql
CREATE TABLE safety_break_event (
    id VARCHAR(50) PRIMARY KEY,
    user_id VARCHAR(50) NOT NULL,
    video_id VARCHAR(50) NOT NULL,
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    violation_type ENUM('missing_ppe', 'unsafe_action', 'other'),
    violation_detail JSON,
    missing_ppe_items JSON,
    confirmation_required BOOLEAN DEFAULT TRUE,
    confirmation_completed BOOLEAN DEFAULT FALSE,
    confirmation_time TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES user(id),
    FOREIGN KEY (video_id) REFERENCES video(id)
);
```

### 8.1.7 动作评分表 (ActionScore) - v6.0新增
```sql
CREATE TABLE action_score (
    id VARCHAR(50) PRIMARY KEY,
    user_id VARCHAR(50) NOT NULL,
    video_id VARCHAR(50) NOT NULL,
    kp_id VARCHAR(50) NOT NULL,
    action_type VARCHAR(100),
    overall_score FLOAT,
    speed_deviation FLOAT,
    angle_deviation FLOAT,
    sequence_correct BOOLEAN,
    deviation_report JSON,                      -- 完整偏差报告
    recommendations JSON,                       -- 改进建议
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES user(id),
    FOREIGN KEY (kp_id) REFERENCES knowledge_point(id)
);
```

### 8.1.8 知识锦囊表 (KnowledgeTip) - v6.0新增
```sql
CREATE TABLE knowledge_tip (
    id VARCHAR(50) PRIMARY KEY,
    kp_id VARCHAR(50) NOT NULL,
    contributor_id VARCHAR(50) NOT NULL,        -- 贡献者
    audio_url VARCHAR(500),                     -- 语音URL
    duration INT,                               -- 时长（秒）
    transcript TEXT,                            -- 转写文本
    status ENUM('pending', 'approved', 'rejected') DEFAULT 'pending',
    reviewer_id VARCHAR(50),
    helpful_count INT DEFAULT 0,
    not_helpful_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (kp_id) REFERENCES knowledge_point(id),
    FOREIGN KEY (contributor_id) REFERENCES user(id)
);
```

### 8.1.9 资源反馈表 (ResourceFeedback) - v6.0新增
```sql
CREATE TABLE resource_feedback (
    id VARCHAR(50) PRIMARY KEY,
    resource_id VARCHAR(50) NOT NULL,
    user_id VARCHAR(50) NOT NULL,
    is_helpful BOOLEAN NOT NULL,
    feedback_text TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (resource_id) REFERENCES resource(id),
    FOREIGN KEY (user_id) REFERENCES user(id),
    INDEX idx_resource_feedback (resource_id, created_at)
);
```

### 8.1.10 认知负荷事件表 (CognitiveLoadEvent) - v6.0新增
```sql
CREATE TABLE cognitive_load_event (
    id VARCHAR(50) PRIMARY KEY,
    user_id VARCHAR(50) NOT NULL,
    video_id VARCHAR(50),
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    load_status ENUM('normal', 'elevated', 'overload') NOT NULL,
    compensation_count_in_window INT,           -- 窗口期内补偿次数
    window_duration_minutes INT DEFAULT 30,
    intervention_type ENUM('rest_suggestion', 'basics_review', 'none'),
    user_response ENUM('accepted', 'ignored', 'pending'),
    FOREIGN KEY (user_id) REFERENCES user(id)
);
```

## 8.2 数据关系图（v6.0升级）

```
User ──┬── CognitiveStateEvent ──→ KnowledgePoint ──→ Video
       │
       ├── RootCauseDiagnosis ──→ KnowledgePoint (trigger & root)
       │
       ├── SafetyBreakEvent ──→ Video
       │
       ├── ActionScore ──→ KnowledgePoint
       │
       ├── KnowledgeTip (contributor) ──→ KnowledgePoint
       │
       ├── ResourceFeedback ──→ Resource
       │
       └── CognitiveLoadEvent

KnowledgePoint ──→ SafetyRegulation (多对多)
KnowledgePoint ──→ ManualSection (一对一)
Resource ──→ KnowledgePoint
```

---

# 9. 接口设计

## 9.1 认知状态相关接口（v6.0新增）

### 9.1.1 上报多模态认知信号
- **接口**: `POST /api/cognitive-state/signals`
- **描述**: 前端实时上报多模态认知信号
- **请求参数**:
```json
{
  "user_id": "U001",
  "video_id": "V001",
  "kp_id": "K3",
  "timestamp": "2024-01-15T10:30:00Z",
  "signals": {
    "gaze_on_screen": true,
    "gaze_position": {"x": 0.5, "y": 0.3},
    "facial_expression": "confused",
    "expression_confidence": 0.88,
    "has_micro_operation": true,
    "body_present": true,
    "pause_duration": 8.5
  }
}
```
- **响应**:
```json
{
  "code": 200,
  "data": {
    "cognitive_state": "difficulty",
    "confidence": 0.92,
    "intervention": {
      "required": true,
      "level": "L1",
      "resource_id": "R001"
    }
  }
}
```

### 9.1.2 获取溯源诊断结果
- **接口**: `GET /api/diagnosis/root-cause/{user_id}/{kp_id}`
- **描述**: 获取某知识点的溯源诊断结果
- **响应**:
```json
{
  "code": 200,
  "data": {
    "trigger_kp": {
      "id": "K5",
      "name": "曲轴拆卸"
    },
    "is_root": false,
    "root_cause": {
      "id": "K2",
      "name": "发动机结构认知"
    },
    "dependency_path": ["K5", "K4", "K3", "K2"],
    "backtrack_depth": 3,
    "recommended_action": "先学习K2，再回到K5"
  }
}
```

## 9.2 安全监控相关接口（v6.0新增）

### 9.2.1 安全检查
- **接口**: `POST /api/safety/check`
- **描述**: 提交视频帧进行安全检查
- **请求参数**:
```json
{
  "video_id": "V001",
  "frame_data": "base64_encoded_frame",
  "operation_type": "welding",
  "timestamp": 125.5
}
```
- **响应**:
```json
{
  "code": 200,
  "data": {
    "is_safe": false,
    "violation_type": "missing_ppe",
    "missing_items": ["goggles", "gloves"],
    "action": "suspend_video",
    "safety_confirmation_required": true,
    "safety_quiz_id": "SQ001"
  }
}
```

### 9.2.2 确认安全规范
- **接口**: `POST /api/safety/confirm`
- **描述**: 学生确认安全规范后继续播放
- **请求参数**:
```json
{
  "user_id": "U001",
  "safety_break_event_id": "SBE001",
  "quiz_answers": [
    {"question_id": "Q1", "answer": "A"},
    {"question_id": "Q2", "answer": "B"}
  ]
}
```

### 9.2.3 获取动作评分
- **接口**: `POST /api/action/score`
- **描述**: 对学生操作视频进行动作评分
- **响应**:
```json
{
  "code": 200,
  "data": {
    "overall_score": 0.82,
    "deviations": {
      "speed": "+15%",
      "angle": "-5°",
      "sequence": "correct"
    },
    "recommendations": [
      "拆卸速度偏快15%，建议放慢节奏",
      "工具握持角度偏离5°，注意调整"
    ],
    "safety_notes": [
      "高速拆卸可能导致零件损坏"
    ]
  }
}
```

## 9.3 社会学习相关接口（v6.0新增）

### 9.3.1 提交知识锦囊
- **接口**: `POST /api/knowledge-tips`
- **描述**: 学生提交经验分享语音
- **请求参数**:
```json
{
  "user_id": "U001",
  "kp_id": "K3",
  "audio_file": "base64_encoded_audio",
  "duration": 28
}
```

### 9.3.2 获取知识锦囊列表
- **接口**: `GET /api/knowledge-tips/{kp_id}`
- **描述**: 获取某知识点的经验分享列表
- **响应**:
```json
{
  "code": 200,
  "data": {
    "tips": [
      {
        "id": "TIP001",
        "contributor_name": "张同学",
        "audio_url": "https://...",
        "duration": 28,
        "helpful_count": 15,
        "transcript": "我当时也卡在这里..."
      }
    ]
  }
}
```

## 9.4 资源反馈接口（v6.0新增）

### 9.4.1 提交资源反馈
- **接口**: `POST /api/resources/{resource_id}/feedback`
- **描述**: 学生对补偿资源进行反馈
- **请求参数**:
```json
{
  "user_id": "U001",
  "is_helpful": false,
  "feedback_text": "解释不够清楚"
}
```
- **响应**:
```json
{
  "code": 200,
  "data": {
    "feedback_recorded": true,
    "resource_status": "active",
    "negative_count": 2,
    "threshold": 3
  }
}
```

## 9.5 认知负荷接口（v6.0新增）

### 9.5.1 检查认知负荷状态
- **接口**: `GET /api/cognitive-load/{user_id}`
- **描述**: 获取学生当前认知负荷状态
- **响应**:
```json
{
  "code": 200,
  "data": {
    "load_status": "elevated",
    "compensation_count": 2,
    "window_minutes": 30,
    "recommendation": "建议放慢学习节奏",
    "next_threshold": 3
  }
}
```

---

# 10. 用户界面设计

## 10.1 学生端界面（v6.0升级）

### 10.1.1 视频播放页（含多模态采集）

```
┌─────────────────────────────────────────────────────────────────────┐
│  导航栏: [首页] [我的学习] [知识锦囊] [个人中心]    📷 摄像头已开启 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────────────────────┐  ┌────────────────────────────────┐│
│  │                            │  │ 📚 知识点目录                  ││
│  │                            │  │ ┌────────────────────────────┐ ││
│  │      视频播放器            │  │ │ ✅ K1: 发动机结构认知      │ ││
│  │                            │  │ │ ✅ K2: 工具准备            │ ││
│  │   【曲轴拆卸操作演示】     │  │ │ ⚠️ K3: 曲轴拆卸 ← 当前    │ ││
│  │                            │  │ │    💡 检测到困难，已推送L1 │ ││
│  │                            │  │ │ ⏸️ K4: 曲轴安装            │ ││
│  │  ▶️ ━━━━━●━━━━━━━━━━━ 🔊   │  │ └────────────────────────────┘ ││
│  └────────────────────────────┘  │                                 ││
│                                   │ ─────────────────────────────  ││
│  ┌────────────────────────────┐  │ 📖 实操手册对照                ││
│  │ 💡 L1提示                  │  │ ┌────────────────────────────┐ ││
│  │ ┌────────────────────────┐ │  │ │ 🔴 安全规范：              │ ││
│  │ │ 曲轴拆卸要点：         │ │  │ │ • 必须佩戴防护手套        │ ││
│  │ │ 1. 先松开固定螺栓      │ │  │ │ • 确保千斤顶稳固          │ ││
│  │ │ 2. 使用专用拆卸工具    │ │  │ │ ⚙️ 参数：扭矩45-50 N·m   │ ││
│  │ │ [查看详情] [已掌握]    │ │  │ └────────────────────────────┘ ││
│  │ └────────────────────────┘ │  └────────────────────────────────┘│
│  └────────────────────────────┘                                      │
│                                                                      │
│  认知状态: 🟡 困难卡顿 | 学习进度: 65% | 认知负荷: 正常              │
└─────────────────────────────────────────────────────────────────────┘
```

### 10.1.2 L2级干预界面

```
┌─────────────────────────────────────────────────────────────────────┐
│  🔧 补偿学习 - 曲轴拆卸                                    [最小化] │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 🎥 精讲微视频 (87秒)                                          │ │
│  │ ┌──────────────────────────────────────────────────────────┐  │ │
│  │ │                                                          │  │ │
│  │ │              [智能剪辑视频播放器]                        │  │ │
│  │ │                                                          │  │ │
│  │ └──────────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 📝 诊断练习 (3题)                                             │ │
│  │                                                                │ │
│  │ 1. 曲轴拆卸时，应先松开哪个部件？                             │ │
│  │    ○ A. 连杆螺栓    ○ B. 主轴承盖    ○ C. 飞轮螺栓           │ │
│  │                                                                │ │
│  │ 2. 拆卸曲轴时的标准扭矩值范围是？                             │ │
│  │    ○ A. 30-35 N·m  ○ B. 45-50 N·m  ○ C. 60-65 N·m            │ │
│  │                                                                │ │
│  │ [提交答案]                                                     │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 💬 知识锦囊 - 同学经验分享                                    │ │
│  │ ┌──────────────────────────────────────────────────────────┐  │ │
│  │ │ 🎤 张同学 (28秒): "我当时也卡在这里，后来发现..."       │  │ │
│  │ │ [▶️ 播放] [👍 15] [👎 2]                                 │  │ │
│  │ └──────────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  这个资源对你有帮助吗？  [👍 有帮助]  [👎 无帮助]                   │
└─────────────────────────────────────────────────────────────────────┘
```

### 10.1.3 安全熔断界面

```
┌─────────────────────────────────────────────────────────────────────┐
│  ⚠️ 安全警告 - 视频已暂停                                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │                                                                │ │
│  │     🚨 检测到安全违规行为                                     │ │
│  │                                                                │ │
│  │     视频中的操作人员未佩戴以下防护装备：                      │ │
│  │     ❌ 护目镜                                                 │ │
│  │     ❌ 防护手套                                               │ │
│  │                                                                │ │
│  │     请先完成安全规范确认，方可继续观看视频。                  │ │
│  │                                                                │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 📋 安全规范确认题                                             │ │
│  │                                                                │ │
│  │ 1. 焊接操作时，必须佩戴护目镜的原因是？                       │ │
│  │    ○ A. 防止飞溅物伤害眼睛                                    │ │
│  │    ○ B. 防止强光损伤视力                                      │ │
│  │    ○ C. 以上都是                                              │ │
│  │                                                                │ │
│  │ 2. 若未佩戴防护手套进行焊接，可能导致？                       │ │
│  │    ○ A. 烫伤    ○ B. 触电    ○ C. 以上都有可能                │ │
│  │                                                                │ │
│  │ [提交确认]                                                     │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  [📺 观看事故案例回顾（可选）]                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 10.1.4 认知负荷预警界面

```
┌─────────────────────────────────────────────────────────────────────┐
│  🧠 学习状态提醒                                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │                                                                │ │
│  │     😓 检测到认知负荷较高                                     │ │
│  │                                                                │ │
│  │     过去30分钟内，你已触发了3次补偿学习。                     │ │
│  │     建议适当休息，避免过度疲劳影响学习效果。                  │ │
│  │                                                                │ │
│  │     ┌────────────────────────────────────────────────────┐   │ │
│  │     │  建议选项：                                        │   │ │
│  │     │                                                    │   │ │
│  │     │  ☕ [休息5分钟]     - 推荐                         │   │ │
│  │     │                                                    │   │ │
│  │     │  📚 [回顾基础知识] - 复习前置知识点               │   │ │
│  │     │                                                    │   │ │
│  │     │  ▶️ [继续学习]     - 我状态良好                    │   │ │
│  │     └────────────────────────────────────────────────────┘   │ │
│  │                                                                │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

## 10.2 教师端界面（v6.0教练模式）

### 10.2.1 教学效果仪表盘（含全班共同盲点）

```
┌─────────────────────────────────────────────────────────────────────┐
│  教学效果仪表盘 - 《汽修-发动机拆装》                    [导出报告] │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────┐  ┌──────────────────────────────────┐│
│  │ 🎯 全班共同盲点          │  │ 📊 认知状态分布                  ││
│  │ (50%以上学生触发补偿)    │  │                                  ││
│  │ ─────────────────────── │  │  深度思考 ████████░░ 40%         ││
│  │ 1. K3 曲轴拆卸    68%   │  │  困难卡顿 ██████░░░░ 35%         ││
│  │    ⚠️ 建议线下重点讲解   │  │  无效停留 █████░░░░░ 25%         ││
│  │ 2. K7 曲轴安装    52%   │  │                                  ││
│  │    ⚠️ 建议线下重点讲解   │  │                                  ││
│  │ ─────────────────────── │  │                                  ││
│  │ [生成重点讲解课件]       │  │                                  ││
│  └──────────────────────────┘  └──────────────────────────────────┘│
│                                                                      │
│  ┌──────────────────────────┐  ┌──────────────────────────────────┐│
│  │ 📈 干预效果统计          │  │ 🔔 待处理事项                    ││
│  │ ─────────────────────── │  │ ─────────────────────────────── ││
│  │ L1干预成功率: 72%        │  │ • 2个L3求助请求待响应            ││
│  │ L2干预成功率: 85%        │  │ • 3个AI资源已自动下架待审核      ││
│  │ L3人类介入: 5次          │  │ • 5个知识锦囊待审核              ││
│  │ ─────────────────────── │  │ ─────────────────────────────── ││
│  │ 平均补偿后掌握率: 78%    │  │ [立即处理]                       ││
│  └──────────────────────────┘  └──────────────────────────────────┘│
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 🔍 溯源诊断分析 - 最常见的根本薄弱点                          │ │
│  │ ─────────────────────────────────────────────────────────────  │ │
│  │ 1. K1 发动机结构认知 (作为根本原因出现 12 次)                 │ │
│  │    → 建议：强化基础理论教学                                   │ │
│  │ 2. K2 工具使用规范 (作为根本原因出现 8 次)                    │ │
│  │    → 建议：增加实操演练环节                                   │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 10.2.2 L3人类介入响应界面

```
┌─────────────────────────────────────────────────────────────────────┐
│  🆘 L3求助请求 - 学生: 李明                              [紧急]     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 📋 学生学习轨迹                                               │ │
│  │ ─────────────────────────────────────────────────────────────  │ │
│  │ • 卡点知识点: K3 曲轴拆卸                                     │ │
│  │ • L1干预: 已完成，未解决                                      │ │
│  │ • L2干预: 已完成，练习正确率 40%                              │ │
│  │ • 溯源诊断: 根本薄弱点为 K1 发动机结构认知                    │ │
│  │ • 回溯补偿: 已完成，但仍未掌握                                │ │
│  │ ─────────────────────────────────────────────────────────────  │ │
│  │ 困难持续时间: 45分钟                                          │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 🎯 响应方式选择                                               │ │
│  │                                                                │ │
│  │  ○ 1对1连线答疑 (推荐)                                        │ │
│  │      [开始视频连线]                                           │ │
│  │                                                                │ │
│  │  ○ 推荐同伴互助                                               │ │
│  │      推荐同学: 张三 (已掌握该知识点，愿意帮助)                │ │
│  │      [发起配对]                                               │ │
│  │                                                                │ │
│  │  ○ 安排线下辅导                                               │ │
│  │      [创建辅导预约]                                           │ │
│  │                                                                │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  [发送鼓励消息]  [查看完整学习记录]  [标记为已处理]                 │
└─────────────────────────────────────────────────────────────────────┘
```

---

# 11. 非功能性需求

## 11.1 性能要求

| 指标 | 要求 | v6.0新增 |
|------|------|---------|
| 视频解析响应时间 | 上传后24小时内完成 | - |
| 行为检测响应时间 | 实时采集，延迟 < 1秒 | - |
| **认知状态判定响应时间** | **≤ 3秒**（3秒窗口期） | ✅ |
| 补偿推送响应时间 | ≤ 3-5分钟 | - |
| **安全熔断响应时间** | **≤ 500ms** | ✅ |
| API接口响应时间 | < 500ms | - |
| **WebSocket消息延迟** | **< 100ms** | ✅ |

## 11.2 准确性要求

| 指标 | 要求 | v6.0新增 |
|------|------|---------|
| 知识点识别准确率 | ≥ 85% | - |
| ASR识别准确率 | ≥ 90% | - |
| OCR识别准确率 | ≥ 85% | - |
| **认知状态分类准确率** | **≥ 85%** | ✅ |
| **PPE检测准确率** | **≥ 90%** | ✅ |
| **动作评分准确率** | **≥ 80%** | ✅ |
| 难点判定准确率 | ≥ 80% | - |

## 11.3 可用性要求

- 系统可用性: ≥ 99%
- 视频播放与事件采集不应明显卡顿
- **摄像头/麦克风权限获取需用户明确授权**（v6.0新增）
- **多模态采集可降级运行**（无摄像头时仅用行为数据）（v6.0新增）

## 11.4 安全性要求

- **隐私保护增强**（v6.0新增）：
  - 多模态数据（面部、视线）仅用于认知状态判定，不存储原始图像
  - 数据本地处理优先，仅上报判定结果
  - 学生可随时关闭摄像头/麦克风
  - 所有隐私数据加密传输和存储
- 权限控制：学生只能查看自己的数据
- API接口需要身份认证

## 11.5 兼容性要求

- 浏览器兼容：Chrome、Firefox、Edge最新版本
- **WebRTC支持**：摄像头/麦克风采集需要WebRTC（v6.0新增）
- 视频格式：支持MP4、WebM
- 响应式设计：支持移动端浏览器访问

---

# 12. 测试与验收标准

## 12.1 功能测试（v6.0新增项）

### 12.1.1 认知状态识别测试
- ✅ 能正确区分"深度思考"、"困难卡顿"、"无效停留"
- ✅ 多模态信号采集正常（视线、表情、鼠标）
- ✅ 3秒窗口期扫描机制正常工作
- ✅ 认知状态分类置信度合理

### 12.1.2 溯源诊断测试
- ✅ 连续失败2次能触发溯源诊断
- ✅ GNN知识图谱路径计算正确
- ✅ 能正确识别根本薄弱点
- ✅ 回补路径生成合理

### 12.1.3 安全熔断测试
- ✅ PPE检测准确（护目镜、手套等）
- ✅ 安全违规能触发视频暂停
- ✅ 安全确认题必须通过后才能继续
- ✅ 安全事件正确记录

### 12.1.4 动作评分测试
- ✅ SlowFast动作轨迹提取正常
- ✅ DTW比对结果合理
- ✅ 偏差报告内容准确
- ✅ 改进建议有针对性

### 12.1.5 社会学习测试
- ✅ 知识锦囊录制和提交正常
- ✅ 审核流程正常工作
- ✅ 推送给卡点学生正常
- ✅ 反馈统计正确

### 12.1.6 分层干预测试
- ✅ L1/L2/L3干预正确触发
- ✅ 干预频率控制正常
- ✅ 认知负荷预警正常触发
- ✅ 人类介入流程正常

### 12.1.7 负反馈闭环测试
- ✅ 资源反馈按钮正常工作
- ✅ 连续3个负反馈自动下架
- ✅ 教师通知正常发送
- ✅ 资源状态正确更新

## 12.2 验收标准总结

**v6.0版本必须满足**：
1. 认知状态识别准确率 ≥ 85%
2. 安全熔断响应时间 ≤ 500ms
3. 分层干预机制正常工作（L1/L2/L3）
4. 溯源诊断能正确识别根本薄弱点
5. 社会学习模块可用
6. 负反馈闭环机制正常
7. 认知负荷预警正常触发
8. PPE检测准确率 ≥ 90%

---

# 13. 项目里程碑与任务规划

## 13.1 v6.0新增任务分配

### 13.1.1 队长（周东吴）新增任务

**认知状态识别模块**（第5-6周）
- [ ] 设计多模态信号融合算法
- [ ] 实现加权概率分类器（BehaviorAnalysisService）
- [ ] 实现3秒窗口期扫描机制

**溯源诊断模块**（第6周）
- [ ] 集成GNN知识图谱
- [ ] 实现最短回溯路径算法
- [ ] 实现根本薄弱点识别逻辑

### 13.1.2 张亚鹏（AI引擎）新增任务

**安全监控模块**（第4-5周）
- [ ] 集成YOLOv8 PPE检测
- [ ] 实现安全熔断逻辑
- [ ] 集成SlowFast动作识别
- [ ] 实现DTW动作比对

**语义增强**（第4周）
- [ ] 集成BERT-base-Chinese
- [ ] 集成Wav2Vec 2.0语气分析

### 13.1.3 成怡（前端）新增任务

**多模态采集**（第4-5周）
- [ ] 集成WebGazer.js视线追踪
- [ ] 集成face-api.js表情识别
- [ ] 实现WebSocket实时通信

**新UI组件**（第6-7周）
- [ ] 安全熔断弹窗
- [ ] 认知负荷预警界面
- [ ] 知识锦囊录制/播放组件
- [ ] L1/L2/L3分层干预界面

### 13.1.4 杨雅娟（后端）新增任务

**新增接口**（第5-7周）
- [ ] 认知状态信号上报接口
- [ ] 安全检查接口
- [ ] 动作评分接口
- [ ] 知识锦囊接口
- [ ] 资源反馈接口

### 13.1.5 雷宇（测试）新增任务

**新增测试用例**（第9-10周）
- [ ] 认知状态识别测试用例
- [ ] 安全熔断测试用例
- [ ] 溯源诊断测试用例
- [ ] 社会学习测试用例

## 13.2 调整后的里程碑

| 里程碑 | 时间 | v6.0新增交付物 |
|--------|------|---------------|
| M2: 解析模块完成 | 第4周末 | + 语气分析、PPE检测 |
| M3: 核心闭环完成 | 第7周末 | + 认知状态识别、安全熔断、分层干预 |
| M4: MVP版本完成 | 第8周末 | + 溯源诊断、社会学习、负反馈闭环 |

---

# 14. 风险与对策

## 14.1 v6.0新增技术风险

### 风险11: 多模态信号采集隐私问题
- **风险描述**: 摄像头/麦克风采集可能引发隐私担忧
- **影响程度**: 高
- **应对策略**:
  - 明确告知用户数据用途
  - 仅上报判定结果，不存储原始图像
  - 支持随时关闭，降级为仅行为数据模式
  - 符合隐私保护法规

### 风险12: 认知状态判定不准确
- **风险描述**: 多模态融合算法可能产生误判
- **影响程度**: 高
- **应对策略**:
  - 初版保守判定，宁可漏判不误判
  - 结合传统行为数据交叉验证
  - 持续收集数据优化模型
  - 支持学生反馈"误判"

### 风险13: 安全熔断过于敏感
- **风险描述**: PPE检测可能产生误报，频繁打断学习
- **影响程度**: 中
- **应对策略**:
  - 设置检测置信度阈值
  - 连续多帧确认后再触发
  - 教师可配置敏感度
  - 提供"豁免"机制（如PPE不在画面内）

### 风险14: GNN知识图谱构建复杂
- **风险描述**: 知识依赖关系难以自动提取
- **影响程度**: 中
- **应对策略**:
  - 初版人工标注依赖关系
  - 提供教师编辑界面
  - 后续迭代自动推断

---

# 15. 附录

## 15.1 术语表（v6.0新增）

| 术语 | 英文 | 说明 |
|------|------|------|
| 信号不协和 | Signal Dissonance | 多模态信号之间的不一致性检测 |
| 认知状态 | Cognitive State | 学习者当前的心理认知状态（深度思考/困难卡顿/无效停留） |
| 溯源诊断 | Root Cause Diagnosis | 通过回溯知识依赖关系找到根本薄弱点 |
| 安全熔断 | Safety Break | 检测到安全违规时强制暂停视频的机制 |
| PPE | Personal Protective Equipment | 个人防护装备（护目镜、手套等） |
| DTW | Dynamic Time Warping | 动态时间规整，用于动作轨迹比对 |
| GNN | Graph Neural Network | 图神经网络，用于知识图谱推理 |
| 知识锦囊 | Knowledge Tip | 同伴分享的学习经验语音 |
| 认知负荷 | Cognitive Load | 学习过程中的心理负担程度 |
| 分层干预 | Layered Intervention | L1/L2/L3三级递进式干预策略 |
| 负反馈闭环 | Negative Feedback Loop | 基于用户反馈自动下架低质量资源的机制 |
| 全班共同盲点 | Class-wide Blind Spots | 50%以上学生触发补偿的知识点 |

## 15.2 技术术语表

| 术语 | 说明 |
|------|------|
| BERT-base-Chinese | 中文预训练语言模型，用于职业术语纠偏 |
| Wav2Vec 2.0 | 语音表征学习模型，用于语气分析 |
| YOLOv8 | 实时目标检测模型，用于PPE检测 |
| SlowFast | 视频动作识别模型，用于动作轨迹提取 |
| WebGazer.js | 浏览器端视线追踪库 |
| face-api.js | 浏览器端人脸识别和表情分析库 |
| PyTorch Geometric | 图神经网络框架 |
| fastdtw | 快速DTW算法实现库 |

## 15.3 配置参数表

### 15.3.1 认知状态判定参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| window_duration | 3秒 | 认知状态扫描窗口期 |
| gaze_offset_threshold | 5秒 | 视线偏移判定阈值 |
| confidence_threshold | 0.7 | 认知状态判定置信度阈值 |
| w1 (认知信号权重) | 0.3 | 困难度计算权重 |
| w2 (行为信号权重) | 0.3 | 困难度计算权重 |
| w3 (知识依赖权重) | 0.2 | 困难度计算权重 |
| w4 (时间异常权重) | 0.2 | 困难度计算权重 |

### 15.3.2 干预策略参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| L1_max_per_kp | 2 | 同一知识点L1干预最大次数 |
| L2_max_per_kp | 1 | 同一知识点L2干预最大次数 |
| L3_max_per_session | 2 | 同一学习周期L3干预最大次数 |
| cognitive_load_window | 30分钟 | 认知负荷监控窗口 |
| cognitive_load_threshold | 3 | 触发认知过载的补偿次数阈值 |
| exercise_pass_rate | 80% | 练习通过率阈值 |

### 15.3.3 安全监控参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| ppe_confidence_threshold | 0.85 | PPE检测置信度阈值 |
| consecutive_frames | 3 | 连续违规帧数确认阈值 |
| safety_quiz_pass_rate | 100% | 安全确认题通过率要求 |

### 15.3.4 资源质量参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| negative_feedback_threshold | 3 | 连续负反馈自动下架阈值 |
| video_max_duration | 90秒 | 补偿微视频最大时长 |
| knowledge_tip_max_duration | 30秒 | 知识锦囊最大时长 |

### 15.3.5 时间戳修正参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| correction_threshold | 30% | 触发修正的用户偏离比例 |
| min_samples | 10 | 触发修正的最小样本数 |
| deviation_threshold | 5秒 | 判定为偏离的时间差阈值 |

## 15.4 待确认问题清单（v6.0新增）

### 15.4.1 技术决策

1. **摄像头权限获取策略？**
   - 强制要求 vs 可选（降级运行）
   - 建议：可选，无摄像头时仅用行为数据

2. **多模态数据本地处理 vs 云端处理？**
   - 隐私考虑 vs 计算能力
   - 建议：优先本地处理，仅上报判定结果

3. **GNN知识图谱初始化方式？**
   - 人工标注 vs 自动推断
   - 建议：初版人工标注，提供编辑界面

4. **安全熔断的触发敏感度？**
   - 宁可误报 vs 宁可漏报
   - 建议：高危场景宁可误报

### 15.4.2 产品决策

5. **知识锦囊是否需要教师审核？**
   - 全部审核 vs 自动审核 + 举报机制
   - 建议：初版全部审核

6. **L3人类介入的响应时效要求？**
   - 实时响应 vs 异步响应
   - 建议：工作时间内30分钟响应

7. **认知负荷预警是强制还是建议？**
   - 强制休息 vs 仅提示
   - 建议：仅提示，尊重学生自主权

## 15.5 参考资源

- 蒋国伟团队前期"教学视频解析"项目经验
- BERT-base-Chinese: https://huggingface.co/bert-base-chinese
- Wav2Vec 2.0: https://github.com/facebookresearch/fairseq
- YOLOv8: https://github.com/ultralytics/ultralytics
- SlowFast: https://github.com/facebookresearch/SlowFast
- WebGazer.js: https://webgazer.cs.brown.edu/
- face-api.js: https://github.com/justadudewhohacks/face-api.js
- PyTorch Geometric: https://pytorch-geometric.readthedocs.io/

## 15.6 联系方式

- **项目组长**: 周东吴
- **技术顾问**: 蒋国伟
- **指导老师**: 王海霞

---

## 文档修订记录

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|----------|
| v1.0 | 2024-01 | 团队 | 第一稿：基础功能模块 |
| v2.0 | 2024-01 | 团队 | 第二稿：明确优先级与核心目标 |
| v3.0 | 2024-01 | 团队 | 第三稿：细化功能设计与数据模型 |
| v4.0 | 2024-01 | 团队 | 第四稿：详细版，补充完整内容 |
| v5.0 | - | - | （跳过） |
| **v6.0** | **2026-01** | **团队** | **第六稿：逻辑漏洞修复、职教特色增强、分层干预** |

### v6.0 详细更新日志

**逻辑重构**：
- 从"数据驱动"转向"认知+情境驱动"
- 引入信号不协和检测（Signal Dissonance）
- 废除单一时间阈值判别

**逻辑漏洞修复**：
- 多模态交叉验证（防止误判走神为困难）
- 负反馈闭环（资源质量保障）
- 时间戳动态修正（解决操作与讲解异步问题）

**职教特色增强**：
- 高危场景安全熔断机制（YOLOv8 PPE检测）
- 基于DTW的动作偏差分析（SlowFast + fastdtw）
- 虚实结合的动作对齐（手册内容实时关联）
- 标准动作库科目选择：汽修

**干预机制优化**：
- 分层化干预策略（L1/L2/L3）
- 防疲劳策略（干预频率控制）
- 认知负荷预警

**新增功能**：
- 社会学习模块（知识锦囊）
- 溯源式根本原因诊断（GNN知识图谱）
- 教练模式（全班共同盲点图表）
- L3人类介入响应

**技术升级**：
- BERT-base-Chinese（语义引擎）
- Wav2Vec 2.0（语气分析，标记考核重点）
- GNN（知识依赖图谱）
- WebSocket（毫秒级同步）

---

**文档结束**

---

**下一步行动**：
1. 团队评审本文档
2. 确认待确认问题清单中的决策
3. 更新任务分配文档
4. 开始v6.0功能开发